<div id="user-inventory">
    <h3 id="user-inventory-heading">{{this.user.name}}'s Inventory</h3>
    <section id="user-inventory-metadata">
        <div id="user-inventory-value" class="user-inventory-metadata-item">
            <p class="user-inventory-metadata-heading">Value</p>
            <p class="user-inventory-metadata-data">{{this.value}}</p>
        </div>
        {{!-- <div id="user-inventory-cost" class="user-inventory-metadata-item">
            <p class="user-inventory-metadata-heading">Cost</p>
            <p class="user-inventory-metadata-data">{{this.cost}}</p>
        </div> --}}
        <div id="user-inventory-quantity" class="user-inventory-metadata-item">
            <p class="user-inventory-metadata-heading">Quantity</p>
            <p class="user-inventory-metadata-data">{{this.quantity}}</p>
        </div>
        {{!-- <div id="user-inventory-gain" class="user-inventory-metadata-item">
            <p class="user-inventory-metadata-heading">Gain(%)</p>
            <p class="user-inventory-metadata-data">{{this.gain}}</p>
        </div> --}}
    </section>

    <table id="user-inventory-cards">
        <thead>
            <tr>
                <th>Owned</th>
                <th>Name</th>
                <th>Set</th>
                <th>Price</th>
                {{!-- <th>Change</th> --}}
                <th>Notes</th>
            </tr>
        </thead>
        <tbody>
        {{#each this.cards}}
            <tr>
                <td>{{>cardsOwned}}</td>
                <td><a href="{{this.url}}">{{this.name}}</a></td>
                <td>
                    <i class="ss ss-{{this.set.keyruneCode}} ss-fw"></i>
                    <a href="{{this.set.url}}">{{this.set.name}}</a>
                </td>
                <td>{{this.price}}</td>
                {{!-- TODO: change in price here --}}
                <td>
                    {{#each this.notes}}
                    <span>{{this.value}}</span>
                    {{/each}}
                </td>
            </tr>
        {{/each}}
        </tbody>
    </table>
</div>