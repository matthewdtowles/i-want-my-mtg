{
  "Name": "IwmmPublicCatalogIdentity",
  "Comment": "Public catalog API paths. Passes the API key and RapidAPI headers to the origin so rate limits and usage counting still see who is calling on a cache miss. Not part of the cache key, so every caller shares the same cached response.",
  "HeadersConfig": {
    "HeaderBehavior": "whitelist",
    "Headers": {
      "Quantity": 3,
      "Items": ["X-API-Key", "X-RapidAPI-Proxy-Secret", "X-RapidAPI-User"]
    }
  },
  "CookiesConfig": { "CookieBehavior": "none" },
  "QueryStringsConfig": { "QueryStringBehavior": "none" }
}
